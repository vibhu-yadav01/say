<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>🧑‍💻 All Users</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="container">
    <h1>🧑‍🤝‍🧑 User Squad</h1>
    <table class="user-table">
      <tr>
        <th>ID</th>
        <th>Username</th>
        <th colspan="2">Options 🛠️</th>
      </tr>
      <% for(user of users){ %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.username %></td>
        <td>
          <form action="/user/<%= user.id %>/edit" method="get">
            <button class="fun-button">✏️ Edit</button>
          </form>
        </td>
        <td>
          <form action="/user/<%= user.id %>/delete" method="get">
            <button class="join-button">🗑️ Delete</button>
          </form>
        </td>
      </tr>
      <% } %>
    </table>
  </div>
</body>
</html>
